# Node modules - 让Docker在容器内重新安装
**/node_modules
**/dist
**/.angular

# Maven target (but keep the final jar)
target/
!target/*.jar

# IDE files
.idea/
.vscode/
*.iml
.settings/
.project
.classpath

# Git
.git/
.gitignore

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Temporary files
*.tmp
*.temp
.cache/

# Docker files (避免递归复制)
Dockerfile*
docker-compose*
*.tar

# Maven wrapper jar (由于权限问题)
.mvn/wrapper/maven-wrapper.jar

# Test results
target/surefire-reports/
target/failsafe-reports/