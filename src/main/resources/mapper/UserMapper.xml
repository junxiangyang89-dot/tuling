<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.webpj.mapper.UserMapper">
    <resultMap id="BaseResultMap" type="com.example.webpj.entity.User">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="username" property="username" jdbcType="VARCHAR"/>
        <result column="password" property="password" jdbcType="VARCHAR"/>
        <result column="email" property="email" jdbcType="VARCHAR"/>
        <result column="phone" property="phone" jdbcType="VARCHAR"/>
        <result column="avatar" property="avatar" jdbcType="VARCHAR"/>
        <result column="status" property="status" jdbcType="TINYINT"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP"/>
        <result column="role" property="role" jdbcType="VARCHAR"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, username, password, email, phone, avatar, status, create_time, update_time, role
    </sql>

    <select id="selectByUsername" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM sys_user
        WHERE username = #{username}
    </select>

    <select id="selectByEmail" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM sys_user
        WHERE email = #{email}
    </select>

    <insert id="insert" parameterType="com.example.webpj.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_user
            (username, password, email, phone, avatar, status, role)
        VALUES
            (#{username}, #{password}, #{email}, #{phone}, #{avatar}, #{status}, #{role})
    </insert>

    <update id="resetPassword" parameterType="com.example.webpj.entity.User">
        UPDATE sys_user
        <set>
            <if test="password != null">password = #{password},</if>
            <!-- 其他可选字段... -->
        </set>
        WHERE username = #{username}
    </update>
</mapper>